# Wildberries Positions Parser 🛒

Асинхронный бот для автоматизации сбора данных с маркетплейса Wildberries. 
Проект выполнен в рамках тестового задания.

## 🚀 Возможности
- **Автоматизация поиска:** Бот самостоятельно вводит поисковый запрос и имитирует нажатие клавиш.
- **Обход защиты:** Использование маскировки браузера и имитация поведения человека (скроллинг, задержки ввода).
- **Парсинг данных:** Сбор SKU, позиции в выдаче и формирование прямых ссылок на товар.
- **Хранение данных:** Интеграция с базой данных **PostgreSQL** через ORM **SQLModel**.
- **Типизация:** Код полностью покрыт аннотациями типов (Type Hinting).

## 🛠 Технологии
- **Python 3.10+**
- **Playwright** (Chromium) — автоматизация браузера.
- **SQLModel** (SQLAlchemy) — работа с БД.
- **PostgreSQL** — хранилище данных.
- **Asyncio** — асинхронное выполнение задач.

## 📦 Установка и запуск

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com
   cd WBparser

2. **Настройка виртуального окружения:**
    ```bash
    python -m venv .venv
    source .venv/bin/activate  # Для Windows: .venv\Scripts\activate
    pip install -r requirements.txt
    playwright install chromium

3. **Настройка базы данных:**
    ```
   Создайте базу данных в PostgreSQL и укажите URL подключения в models.py:
   DB_URL = "postgresql://user:pass@localhost:5432/dbname"

4. **Запуск:**
   ```bash
   python main.py